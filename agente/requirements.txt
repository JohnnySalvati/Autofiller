# Agente local de AutoFiller: es lo unico que toca SISalud.
fastapi>=0.115
uvicorn[standard]>=0.30

# Playwright se engancha por CDP al Chrome del operador, asi que NO hace falta
# correr "playwright install": no usa los navegadores que descarga, solo el
# driver que viene en el paquete.
playwright>=1.45

# El agente vive en la bandeja del sistema: un icono al lado del reloj en vez de
# una ventana de consola abierta todo el dia. pystray necesita Pillow para la
# imagen del icono. El visor del log usa Tkinter, que viene con Python.
pystray>=0.19
pillow>=10
