venv
.venv/
*/.venv/
*.xlsx
build
dist
__pycache__/
decrypted.pdf
# Facturas de prueba: contienen nombre y DNI de afiliados reales, no van al repo.
samples/
# Claves de API (ANTHROPIC_API_KEY): nunca al repo.
.env
*/.env
# Prueba de OCR: texto de comprobantes con datos reales de afiliados.
pruebas/cache_ocr/
pruebas/resultado_ocr.md

# Empaquetado del agente (PyInstaller): se reconstruye, no se versiona.
agente/build/
agente/dist/
agente/AutoFillerAgente.spec
agente/AutoFillerAgente.zip
agente/AutoFillerAgente.json

# El agente publicado para que las PCs se actualicen solas. En la VM esto es la
# carpeta que se monta en el contenedor y se llena por scp; en desarrollo, la que
# sirve el servidor local. Son binarios compilados: no van al repo.
publicacion/

# Configuracion local de Claude Code (permisos de esta PC): no va al repo.
.claude/
